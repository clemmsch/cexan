name: 🔥 Merge Devel and Main

on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: 🎭 Creating Pull Request
        id: open-pr
        uses: repo-sync/pull-request@v2.6
        with:
          source_branch: "devel"
          pr_title: "Automatic Pull Request - Merging devel -> main"
          pr_body: ":rocket: It is that time again! The branch 'devel' is being be merged into 'main'. Let's hope that devel is stable :slightly_smiling_face: . :exclamation: Please do not interfer with this pull request! "
          pr_reviewer: "peachmage, somekindofpanem, aemreaydin, BrettBelanger"
          pr_label: automerge
          pr_allow_empty: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: 🧮 Output Pull Request Details
          run: |
            echo Add other actions to build,
            echo test, and deploy your project.

          #run: |
          #  echo Pull-Request-URL
          #  echo ${{steps.open-pr.outputs.pr_url}}
          #  echo Pull-Request-Number
          #  echo ${{steps.open-pre.outputs.pr_number}}

